intent: "Delete an entry, guarded by modifyAllowed"
api_mode: flexicon
write_enabled: true
session:
  discovered_apis: ["LexEntryOperations.Delete"]
code: |
  from flexicon import LexEntryOperations

  def Main(project, report, modifyAllowed):
      if modifyAllowed:
          project.LexEntry.Delete(some_entry)
      report.Info("done")

  docs = {"FTM_Name": "x", "FTM_Synopsis": "x", "FTM_ModifiesDB": True, "FTM_Description": "x"}
  FlexToolsModule = FlexToolsModuleClass(Main, docs)
expect:
  outcome: ok
  error_code: null
  auto_fixes: 0
notes: "DELETE-operation counterpart to 02_ok_guarded_write_create.yaml."
