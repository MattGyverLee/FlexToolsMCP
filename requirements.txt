# MCP Server (1.27.0+ required for anyio compatibility)
mcp>=1.27.0
anyio>=4.5
httpx>=0.27.1
typing-inspection>=0.4.1

# Semantic Search
sentence-transformers>=2.2.0
faiss-cpu>=1.7.4
# Or use chromadb>=0.4.0 as alternative

# .NET Interop (for LibLCM extraction)
pythonnet>=3.0.0

# FieldWorks wrappers the server generates scripts against. pyflexicon is the
# deep wrapper; flexlibs is the shallow/stable one -- small, pythonnet-only, and
# shipped so the stable-flexlibs index matches out of the box and can refresh.
pyflexicon>=4.3.0,<5
flexlibs>=1.2.8,<2

# Utilities
pydantic>=2.0.0
python-dotenv>=1.0.0

# Development
pytest>=7.0.0
pytest-asyncio>=0.21.0
pre-commit>=4.0.0
