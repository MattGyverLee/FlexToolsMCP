[pytest]
asyncio_mode = auto
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
markers =
    requires_flex: mark test as requiring a live FieldWorks installation (deselect on Linux CI with -m "not requires_flex")
