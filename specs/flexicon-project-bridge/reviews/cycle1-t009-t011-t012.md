# T009 / T011 / T012 -- CloseProject/SaveChanges attached-view guards (GREEN)

**File touched:** `D:/Github/_Projects/_LEX/flexicon/flexicon/code/FLExProject.py` (only file
written this cycle, per the brief -- `undoable_operation.py` is owned by another agent this
cycle and was not touched; it appeared already modified in the working tree before I started,
consistent with concurrent T010 work).

## Insertion points (file:line, after edits)

- **T009 -- `CloseProject` no-op guard:** `FLExProject.py:562-568`, inserted after the
  docstring's closing paragraph (a new short paragraph noting the no-op, `:562-566`) and
  immediately before the pre-existing `if hasattr(self, "project"):` (`:575`). Guard body:
  ```python
  if _IsAttachedView(self):
      logging.getLogger(__name__).debug(
          "CloseProject: no-op on a project attached with "
          "FromOpenProject(); the host owns this cache and its "
          "lifecycle."
      )
      return None
  ```
  Never raises; returns before `HasOpenSessionTask()`/`EndNonUndoableTask()`/`usm.Save()`/the
  `finally: self.project.Dispose(); del self.project` block, so `CloseProject()` on a view
  never reaches any of those.

- **T011 -- `SaveChanges` attached-view guard:** `FLExProject.py:1076-1077`, inserted as the
  very first statement of the body (right after the closing `"""`), before the pre-existing
  `if not self.writeEnabled: raise FP_ReadOnlyError()` (now `:1079-1080`) and therefore before
  the `CurrentDepth` read and the issue-#243 `depth > 0` guard (now `:~1101`):
  ```python
  if _IsAttachedView(self):
      raise FP_RuntimeError(_ATTACHED_VIEW_SAVE_REFUSAL)
  ```
  Raises the exact `FP_RuntimeError` type using the shipped `_ATTACHED_VIEW_SAVE_REFUSAL`
  constant verbatim (no inline prose added, no `CloseProject()` advice repeated).

- **T012 -- `SaveChanges` docstring:** `FLExProject.py:1035-1054` (the `Note:`/`Raises:`
  blocks). One pre-existing line was reworded to scope it to owned projects (see "Existing
  lines changed" below); a new one-line attached-view paragraph and a new `Raises:` bullet for
  `FP_RuntimeError` were added.

## Full `git diff -U0` of `FLExProject.py`

```diff
diff --git a/flexicon/code/FLExProject.py b/flexicon/code/FLExProject.py
index f94e8b4..2765967 100644
--- a/flexicon/code/FLExProject.py
+++ b/flexicon/code/FLExProject.py
@@ -119,0 +120,63 @@ def OpenProjectInFW(projectName):
+# -----------------------------------------------------------
+#
+# Attached views -- FLExProject.FromOpenProject()
+#
+# A FLExProject can be reached two ways: it can OWN a project it opened
+# itself (OpenProject), or it can be an ATTACHED VIEW over a cache some
+# other host already opened (FromOpenProject). The two differ in exactly
+# one observable: an attached view carries `_attached_donor`.
+#
+# The constants below are the shared, ASCII-only refusal texts for the
+# lifecycle paths a view must not take. They live at module scope so
+# undoable_operation.py can reach them through the same lazy
+# import-inside-the-method it already uses for the exception classes
+# (FLExProject imports undoable_operation at module level, so the reverse
+# import can only ever be lazy).
+
+
+def _IsAttachedView(obj):
+    """
+    Is `obj` a FLExProject attached to a cache someone else opened?
+
+    Invariant A (specs/flexicon-project-bridge/data-model.md section 1):
+    `_attached_donor` is present iff the instance is an attached view, and
+    every lifecycle guard branches on THIS -- never on `writeEnabled` and
+    never on `_undoable`. Those two cannot carry the distinction: a
+    read-only owned project and a read-only view agree on `writeEnabled`,
+    and an owned project opened with `undoable=False` agrees with every
+    view on `_undoable`.
+    """
+
+    return hasattr(obj, "_attached_donor")
+
+
+# The host owns the save. Raised by SaveChanges() on a view.
+#
+# Deliberately does NOT repeat the standing "Use CloseProject() instead"
+# advice that the owned-project path gives: on a view CloseProject() is a
+# silent no-op, so a caller who followed it would get a run that reports
+# success and writes nothing -- the exact failure class this seam exists
+# to end (research.md R7).
+_ATTACHED_VIEW_SAVE_REFUSAL = (
+    "SaveChanges() is not available on a project attached with "
+    "FromOpenProject(). This object is a view over a cache the host "
+    "(FLExTools, or FieldWorks itself) opened and still owns, and the "
+    "host saves it on its own schedule. Make your changes inside "
+    "Transaction() and simply return from Main() -- do not save, and do "
+    "not close."
+)
+
+# Phase 2 is unavailable on a view. Used by undoable_operation.py.
+#
+# The owned-project wording ("opened with undoable=False") is actively
+# misleading here: nobody called OpenProject, and no argument the module
+# could pass would change the answer (research.md R2).
+_ATTACHED_VIEW_UNDOABLE_REFUSAL = (
+    "UndoableOperation() is not available on a project attached with "
+    "FromOpenProject(). The host opened a session-long non-undoable task "
+    "over this cache, and starting an undoable unit of work inside it "
+    "would nest the wrong kind of task. Use Transaction() instead, which "
+    "is the supported construct on an attached view."
+)
+
+
@@ -324,0 +388,143 @@ class FLExProject(object):
+    @classmethod
+    def FromOpenProject(cls, donor):
+        """
+        Attach a flexicon facade to a project someone else already opened.
+
+        `donor` is whatever the host handed `Main()`: under real FLExTools a
+        **flexlibs** ``FLExProject`` (flextoolslib/code/FTModules.py:75), under
+        the FLExTools MCP a **flexicon** one. Returns an object exposing the
+        full flexicon facade over the donor's cache.
+
+        **Never opens a project, and never closes one.** Reopening a project
+        the host is holding raises ``FP_FileLockedError``, which is the trap
+        any "just make your own flexicon project" advice walks into.
+
+        The portable module shape -- identical under both hosts::
+
+            from flexicon import FLExProject
+
+            def Main(project, report, modifyAllowed):
+                fx = FLExProject.FromOpenProject(project)
+                lex, variants = fx.LexEntry, fx.Variants
+
+        Importing the class is not enough on its own: it does not change the
+        instance ``Main()`` is handed. This classmethod is what makes that
+        import load-bearing.
+
+        Notes:
+
+        * **Idempotent.** A donor that is already a flexicon ``FLExProject``
+          is returned unchanged, by identity -- it owns its project, and a
+          module written against this seam must stay correct under the MCP.
+        * **Phase 1 only.** The view is always ``_undoable = False``; the host
+          owns the transaction envelope. Use ``Transaction()``;
+          ``UndoableOperation()`` is refused.
+        * **Owns nothing.** ``CloseProject()`` on the returned view is a
+          no-op and ``SaveChanges()`` is refused. The host saves.
+
+        Raises:
+            FP_ParameterError: the donor is missing the cache or
+                ``writeEnabled``. The message names every absent attribute
+                and the donor's module.
+        """
+
+        # Idempotence FIRST, before validation -- a flexicon donor is already
+        # exactly what the caller asked for, and re-attaching would silently
+        # downgrade an MCP session from Phase 2 to Phase 1 while leaving two
+        # objects believing they own one cache.
+        if isinstance(donor, cls):
+            return donor
+
+        cls._ValidateDonor(donor)
+
+        # Allocate WITHOUT calling __init__: this class deliberately has none
+        # (all state is born in OpenProject), and adding one now would be a
+        # breaking change for every caller that constructs FLExProject() with
+        # no arguments -- the documented construction.
+        view = cls.__new__(cls)
+
+        view.project = donor.project
+
+        # Prefer the donor's own handles so the view points at the very
+        # objects the host is using; fall back to deriving them from the
+        # cache so a thinner donor still works.
+        lp = getattr(donor, "lp", None)
+        view.lp = lp if lp is not None else donor.project.LangProject
+
+        lexDB = getattr(donor, "lexDB", None)
+        view.lexDB = lexDB if lexDB is not None else view.lp.LexDbOA
+
+        # Verbatim, including False. A view never upgrades its permissions.
+        view.writeEnabled = donor.writeEnabled
+
+        # Unconditionally Phase 1, regardless of the donor's own mode (a
+        # flexlibs donor has no mode at all -- it has no _undoable). When the
+        # host is write-enabled it holds a session-long BeginNonUndoableTask()
+        # envelope, and an undoable unit of work inside that is the wrong kind
+        # of nesting.
+        view._undoable = False
+
+        # The mode discriminator every lifecycle guard branches on.
+        view._attached_donor = donor
+
+        logging.getLogger(__name__).debug(
+            "FromOpenProject: attached a view to a cache owned by %s "
+            "(writeEnabled=%s)",
+            type(donor).__module__,
+            view.writeEnabled,
+        )
+
+        return view
+
+    @classmethod
+    def _ValidateDonor(cls, donor):
+        """
+        Refuse a donor we cannot build a view over, AT the seam.
+
+        The failure this replaces is an ``AttributeError`` fifty frames deep
+        inside an operation -- unreadable to the non-programmer this package
+        exists for (SPEC 3c). Collecting *every* missing attribute into one
+        message beats failing on whichever happens to be checked first: a
+        donor that is wrong is usually wrong in more than one way, and one
+        round-trip per missing attribute is a bad way to learn that.
+
+        Only `project` and `writeEnabled` are required. `lp` and `lexDB` are
+        derived from the cache when the donor does not carry them, so their
+        absence is not an error.
+
+        Raises:
+            FP_ParameterError: naming every absent attribute and the donor's
+                module.
+        """
+
+        missing = []
+
+        # None counts as absent: a donor carrying project=None is no more
+        # usable than one with no `project` at all, and installing that None
+        # would just move the AttributeError deeper.
+        if getattr(donor, "project", None) is None:
+            missing.append("project")
+
+        # Presence only -- the VALUE is borrowed verbatim, and False is a
+        # perfectly good answer (a read-only host).
+        if not hasattr(donor, "writeEnabled"):
+            missing.append("writeEnabled")
+
+        if not missing:
+            return
+
+        # type(donor).__module__ rather than the class name: BOTH candidate
+        # donor classes are named "FLExProject" (flexlibs.code.FLExProject vs
+        # flexicon.code.FLExProject), so the name discriminates nothing and
+        # the module is the only honest answer to "what did I actually get?".
+        raise FP_ParameterError(
+            "FromOpenProject() cannot attach to this object: it is missing "
+            "{missing}. Pass the project your module was handed as Main()'s "
+            "first argument. (Received an object of type {name} from module "
+            "{module}.)".format(
+                missing=", ".join(missing),
+                name=type(donor).__name__,
+                module=type(donor).__module__,
+            )
+        )
+
@@ -355,0 +562,5 @@ class FLExProject(object):
+
+        Attached-view no-op: on a project attached with
+        ``FromOpenProject()``, this method does nothing and returns
+        ``None`` -- the host owns the cache, and a defensive close in an
+        otherwise-correct module must not fail (SPEC 3b).
@@ -356,0 +568,7 @@ class FLExProject(object):
+        if _IsAttachedView(self):
+            logging.getLogger(__name__).debug(
+                "CloseProject: no-op on a project attached with "
+                "FromOpenProject(); the host owns this cache and its "
+                "lifecycle."
+            )
+            return None
@@ -818 +1036 @@ class FLExProject(object):
-            Only valid for write-enabled projects.
+            Only valid for write-enabled, owned projects.
@@ -828,0 +1047,2 @@ class FLExProject(object):
+            Attached view (FromOpenProject()): host owns save, return.
+
@@ -829,0 +1050 @@ class FLExProject(object):
+            FP_RuntimeError: Attached view.
@@ -854,0 +1076,3 @@ class FLExProject(object):
+        if _IsAttachedView(self):
+            raise FP_RuntimeError(_ATTACHED_VIEW_SAVE_REFUSAL)
+
```

(T002/T005/T006/T007 -- `_IsAttachedView`, the two refusal constants, and
`FromOpenProject`/`_ValidateDonor` -- were already present on the branch before this cycle,
per the brief; they show up in this diff only because it is relative to `HEAD` on this
feature branch, not because I touched them this cycle. My own edits this cycle are limited to
the four hunks at `:562` (T009 docstring line), `:568` (T009 guard), `:1036`/`:1047`/`:1050`
(T012 docstring), and `:1076` (T011 guard).)

## Existing lines changed

**One**, and it is inside the `SaveChanges` docstring, which T012 explicitly scopes for
editing ("Docstring text only -- no executable change"):

```
-            Only valid for write-enabled projects.
+            Only valid for write-enabled, owned projects.
```

Every other hunk in the diff is a pure insertion (`@@ -N,0 +M,K @@` -- zero old lines,
K new lines). In particular:
- Inside `CloseProject`'s body: **zero** existing lines changed. The guard is inserted whole
  ahead of the pre-existing `if hasattr(self, "project"):`, which is untouched, and every
  line inside the `try/except/finally` below it (the `#243` `HasOpenSessionTask()` guard,
  the `EndNonUndoableTask()` call, `usm.Save()`, and the `finally: self.project.Dispose();
  del self.project`) is byte-identical to `HEAD`.
- Inside `SaveChanges`'s body: **zero** existing lines changed. The guard is inserted whole
  ahead of the pre-existing `if not self.writeEnabled:`, which is untouched, and the depth
  read, the `depth > 0` branch (both the `_undoable` and non-`_undoable` `FP_TransactionError`
  messages, verbatim, spec.md C20/C21 prose included), and the trailing `usm =
  self.ObjectRepository(IUndoStackManager); usm.Save()` are all byte-identical to `HEAD`.

## A note on the SaveChanges docstring edit and `test_transaction_honesty.py`

`tests/test_transaction_honesty.py::TestRefreshFromDisk::test_uses_same_accessor_pattern_as_save_changes`
does a fixed 6000-character source-text window from `def SaveChanges(self):` and asserts
`self.ObjectRepository(IUndoStackManager)` appears inside that window. My first draft of the
T012 docstring addition (a longer attached-view paragraph plus a longer `Raises:` bullet)
pushed that accessor call to character offset ~6501-6543 -- past the window -- and turned this
pre-existing, unrelated-looking test RED. I confirmed this was caused by my own docstring
growth (not by T009/T010/T011) by `git stash`-ing my changes and re-running the isolated
failures: the test passed clean on `HEAD`. I then trimmed the added `Note:`/`Raises:` prose to
the minimum that still satisfies T012's three requirements (scope the `CloseProject()`
sentence to owned projects; add a short attached-view paragraph naming `FromOpenProject()`;
add a `Raises:` bullet for `FP_RuntimeError`) so the accessor call lands at offset 5956-5996,
inside the window with a small margin. This is documented here rather than silently fixed
because it explains why the final `SaveChanges` docstring wording is terser than a first draft
would produce, and because the margin (4 characters) is thin enough that a future docstring
edit to this method should re-check this test rather than assume headroom.

## Verification

1. `python -m pytest tests/test_from_open_project.py -q`
   -> **38 passed** (23 pre-existing + 15 from T008, all green; matches the T008 RED report's
   8 target failures now flipped to green, plus the 7 already-green new tests and 23
   regression locks still green).

2. `python -m pytest -q` (full offline gate, no markers)
   -> **2462 passed, 9 failed, 23 skipped, 2 xfailed, 56 warnings, 5 subtests passed** in
   ~209s.
   The brief's stated baseline (1748 passed / 710 deselected) does not match this repo's
   current bare `python -m pytest -q` invocation shape (that count corresponds to an older
   `-m "not requires_live_project"` run recorded in a different spec's evidence, and this repo
   has grown since); I verified against the counts actually observed on this branch instead.
   All 9 failures were confirmed **pre-existing and unrelated** to this cycle's diff by
   `git stash`-ing `FLExProject.py`/`FLExProject.pyi`/`undoable_operation.py` and re-running
   each failing test in isolation: all 9 fail identically with my changes stashed out.
   - 7x `flexicon/sync/tests/test_duplicate_operations.py::TestLexEntryDuplicate::*` -- fail
     identically stashed and unstashed; unrelated to `FromOpenProject`/`CloseProject`/
     `SaveChanges`.
   - 1x `tests/operations/test_natural_classes.py::TestNaturalClassSync::
     test_apply_raises_on_type_mismatch_segments_target` -- fails identically stashed and
     unstashed.
   - 1x `tests/operations/test_text_operations.py::TestTextOperationsIntegration::
     test_create_and_delete_text` -- a live-FieldWorks integration test failing on
     `FP_ParameterError: A text with the name 'Test Text 123' already exists`, i.e. leftover
     state from a prior run of the same live scratch project; reproduced identically with my
     changes stashed out, so it is pre-existing test-order/state pollution, not a regression.
   A first full run (before the docstring trim described above) additionally showed 3 more
   failures that were genuinely caused by my initial draft --
   `tests/operations/test_issue243_closeproject_probe.py::test_p4_control_run_normal_close`,
   `tests/operations/test_undoable_mode_live.py::TestPersistenceAcrossReopen::
   test_per_operation_writes_persist_without_an_explicit_block`, and
   `tests/test_transaction_honesty.py::TestRefreshFromDisk::
   test_uses_same_accessor_pattern_as_save_changes` -- all three are now green after the
   docstring trim; see the section above.

3. `git diff -U0 flexicon/code/FLExProject.py` -- reproduced in full above; every hunk outside
   the SaveChanges docstring is a pure insertion, and the one modified line is the scoped
   `Only valid for write-enabled, owned projects.` sentence T012 explicitly authorizes editing.

## Constraints honored

- Only `flexicon/code/FLExProject.py` was written this cycle; `undoable_operation.py` was not
  touched (it appears modified in `git status` from the other agent's concurrent T010 work,
  not from this session).
- `CloseProject()` never raises on an attached view; it returns `None` before
  `HasOpenSessionTask()`/`EndNonUndoableTask()`/`usm.Save()`/`Dispose()`/`del self.project`.
  Debug-level log names `FromOpenProject`.
- `SaveChanges()` raises `FP_RuntimeError` exactly (not `FP_ReadOnlyError`, not
  `FP_TransactionError`), checked before both the `writeEnabled` check and the issue-#243
  depth guard, using the shipped `_ATTACHED_VIEW_SAVE_REFUSAL` constant verbatim -- no
  `CloseProject()` advice was inlined.
- The `SaveChanges` docstring's owned-project `CloseProject()` sentence is now scoped, with a
  short attached-view paragraph naming `FromOpenProject()` and a `Raises:` entry for
  `FP_RuntimeError` added beside it; no other docstring paragraph was rewritten.
- ASCII only throughout (no non-ASCII characters were introduced).
- Did not commit.
