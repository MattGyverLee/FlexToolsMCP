# T024 report -- check_active_parser (parser_probe.py)

**Signature/behaviour landed:** `check_active_parser(project, *, supported_engines: Tuple[str, ...] = ("HC",)) -> None`. Reads `project.MorphologicalDataOA.ActiveParser` once, does a case-sensitive `in supported_engines` membership test, returns `None` on match. On mismatch raises `ParserEngineMismatchError(detail)`, a new exception whose `.detail` is a plain dict shaped exactly like `response_models.ParserEngineMismatchDetail` (`error_code`, `configured_engine`, `supported_engines` as a list, non-empty `hint`) -- round-trips through `validate_detail()` unchanged, matching T006's precedent.

**Corrupt-XML fail-safe:** implemented by doing nothing locally. `check_active_parser` performs no XML parsing at all -- it only reads the already-resolved `ActiveParser` string property, whose corrupt-XML -> `"XAmple"` default lives entirely inside FieldWorks' own C# getter (`OverridesLing_MoClasses.cs:4213`). Because there is no local fallback branch here that could independently decide what to do with a parse failure, there is no code path in this function that could be miswired to default to `"HC"` -- the only way it ever sees anything besides `"XAmple"`/`"HC"` is if the live property itself returns something else, which is exactly what property 2's closed-set comparison then refuses via the ordinary mismatch path.

**No caching guarantee:** `configured_engine` is a single local read per call, taken directly off `project.MorphologicalDataOA.ActiveParser` on every invocation. Deliberately not used: no module-level dict/cache, no `functools.lru_cache` or `functools.cache` decorator, no per-project or per-session memo attribute, no closure capturing a prior result. Each call re-touches the live property; `TestLiveRereadNeverCachedPerSession`'s `read_count` assertions confirm this directly.

**Handler wiring:** none. `check_active_parser` has zero callers anywhere in `src/flextoolsmcp/server/**` at this commit -- grep confirms no reference outside `parser_probe.py` itself and the test file. CP2 adds the first caller.

**test_parser_engine_gate.py:** before 4 passed / 16 failed -> after **20 passed / 20**.

**Full-suite delta:** `test_parser_probe.py` stays 15/15 (unaffected). `test_mcp_tools.py` stays 30/30. Full suite: 56 failed / 1562 passed / 9 skipped -- all 56 failures are the documented pending-task RED (`test_parser_agent_probe.py` T025, `test_parser_health_block.py` T012/T013, `test_grammar_scan_checks.py` + part of `test_grammar_health.py` T019/T034/T035/T020, including one `ImportError: cannot import name 'grammar_health'` confirming that suite's failures are pre-existing missing-handler collection errors, not regressions from this change). `ruff check src/flextoolsmcp/server/parser_probe.py`: all checks passed.
