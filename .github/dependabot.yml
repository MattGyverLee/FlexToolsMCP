version: 2
updates:
  - package-ecosystem: "github-actions"
    directory: "/"
    schedule:
      interval: "monthly"

  - package-ecosystem: "pip"
    directory: "/"
    schedule:
      interval: "monthly"
    # Track dev dependencies only; runtime deps are capped in pyproject.toml
    allow:
      - dependency-type: "development"
