{
  "feature": "shared-mode-access",
  "status": "needs_human",
  "last_cycle": 7,
  "spurts_completed": 4,
  "branch": "main + 7 local commits (6677fd8, 520dba4, ad1d50c, d0b360f, d278cc6, 7a4fa5c, + the cycle-7 close commit = HEAD -- NOT pushed; no PR open for #93)",
  "branch_tip": "the cycle-7 close commit (HEAD); code tip is 7a4fa5c",
  "main_at": "433f3a0",
  "tasks_done": [
    "T1.1 -- undoable=False hardcoded at the generated OpenProject call; undoable session flag + plumbing removed",
    "T1.2 -- flextools_undo_last_operation removed (tool def, dispatch entry, input model, handler, undo_subprocess.py)",
    "T1.3 -- false cross-session-reverse warning deleted; undo_available/redo_available dropped from get_session_history",
    "T1.4 -- dead Feature-3 undo machinery deleted (record_operation, undo/redo stacks, undo_checkpoints + rollover logging)",
    "T1.5 -- success no longer reported over a run that emitted report.Error",
    "T1.6 -- tests/test_undo_wiring.py deleted; tests/test_issue92_write_path_e2e.py added (requires_flex, skips by default)",
    "T6.7 -- ReportedError retry contract written into SPEC.md under CP6 (from cycle2-domain)",
    "SPEC.md Section 8 'Deferred follow-ups' added",
    "CP2 T2.1-T2.2 -- src/flextoolsmcp/server/project_access.py: read_lock_holder, probe_project_access, _pid_is_alive, is_project_sharing_enabled, build_access_remedy; tests/test_shared_mode_access.py (30 tests) [39d1caf]",
    "CP3 T3.5 -- response_models.py:293-301 ProjectLockedDetail carries all five probe fields (verdict, sharing_enabled, holder_pid, holder_process, remedy); extra=forbid",
    "CP4 -- shared-mode write gate at handlers/execution.py:4091-4251 + advisory :4360; tests/test_shared_mode_write_gate.py (15 tests) [6dc459a, re-confirmed db52c3a]",
    "CP6 T6.5 -- CHANGELOG.md [Unreleased] entries for #92/#93",
    "Out-of-band -- nested-UoW pre-flight gate: validators.py:444-548 detect_nested_unit_of_work, 17th error code nested_unit_of_work, tests/test_nested_uow_gate.py [250ca52]",
    "CP3 T3.1-T3.4 -- _diagnose_project_open_error (execution.py:1195-1265) now calls probe_project_access and emits verdict-specific hints; new build_lock_diagnosis() sibling in project_access.py:277-320 (build_access_remedy deliberately NOT widened: test_shared_mode_write_gate.py:77-80 asserts it returns None for stale_lock); tests/test_cp3_lock_diagnosis.py (6 tests) [6677fd8]",
    "Cycle 6 -- CP3 hardening [520dba4]: atomic extra/diag.update, guarded logger.debug on the swallow, tightened PID assertions, tests/test_cp3_lock_diagnosis.py -> tests/test_shared_mode_lock_diagnosis.py, project_access.py:283 pointer corrected to 4203-4299. QC CONFIRMS P1-1 CLOSED (diag is genuinely atomic).",
    "Cycle 6 -- sibling sweep [ad1d50c]: additive verdict/sharing_enabled/blocking on the validate_only project_lock (execution.py:2049-2076); project_discovery.py sweep_stale_locks dead-holder branch reworded. INCOMPLETE -- see p1_open.",
    "Cycle 6 -- SPEC.md amended by lex-doc (Section 3 split Class A/B + unclassified + unreachable; T5.1/T5.3/T5.5 drift fixed; new T5.6; Section 8 += P2-4, P2-8). UNCOMMITTED at time of writing -- cycle 7 archivist task.",
    "Cycle 7 -- SPEC.md Section 3/CP5 amendment COMMITTED by lex-archivist [d0b360f]: 10 files (SPEC.md, .crew-handoff.json, seven cycle5/cycle6 reviews, STATUS.md). Zero src/ or tests/ files staged; .spec-context.json and .specify/ deliberately untouched.",
    "Cycle 7 -- MECHANICAL LOCK-SITE INVENTORY produced: reviews/lock-site-inventory.md (38 sites: 26 src/, 12 tests/) + reviews/cycle7-inventory.md (defect rows only). Greps reproduced the 26/21/152 baseline EXACTLY. Both known defects surfaced INDEPENDENTLY, plus TWO the three judgement-based sweeps never named (project_discovery.py:344-354; the false-justification test at test_shared_mode_access.py:442, whose cited dependency in test_startup_lock_sweep.py has ZERO occurrences of 'Stale lock detected'). The method is VINDICATED -- keep it.",
    "Cycle 7 -- all six programmer items landed [7a4fa5c]: P1 project_discovery unknown-holder rewording (inventory's exact text), P1-companion test renamed to test_unreadable_lock_is_reported_as_exclusively_held with the false docstring struck, P1-A validate_only enrichment test coverage (TestValidateOnlyProjectLockEnrichment, 4 tests) + probe_project_access stubbed in _stub_validate_only_env/_stub_agreement_env, fail-open interim patch (ProjectAccess.probed), Dict[str, Any] annotation, and the P2 live-holder branch now branching on _is_fieldworks_process_name + is_project_sharing_enabled and delegating held_by_other wording to build_access_remedy. Tests 1144 -> 1148 passed / 4 skipped; validate_integrity.py all exit 0.",
    "Cycle 7 -- lex-lead read the 7a4fa5c diff itself and re-ran the five lock-related test files (83 passed, 2 subtests) rather than dispatching a fourth judgement pass over a bug class that now has a mechanical artifact.",
    "Cycle 7 (user-directed, outside the plan) -- STATUS.md self-contradiction struck [d278cc6]: item 5 listed the CastingOperations.cast_to_concrete phantom remedy as OPEN while the same file already recorded it fixed and filed in 1f8e90b as #112/#113. Re-verified: zero CastingOperations occurrences in src/ or docs/; surviving advertisements name flexicon.code.lcm_casting; cast_to_concrete is real and unary at flexicon/code/lcm_casting.py:408. Two dependent counts corrected.",
    "Cycle 7 -- specs/shared-mode-access/evidence/live-session-checklist.md written: the SINGLE consolidated, ordered live checklist (pre-flight + backup, CP4-a/b/c, CP3 sharing-off both parts, OPEN Q1/Q2/Q3, wrap-up) with exact commands and per-item pass criteria. CP5-a is explicitly Session 2, after the CP5 gate exists.",
    "Cycle 7 -- SPEC.md Section 8 extended with four deferred entries: the fail-open issue (with the recommended probed/unknown/always-emit-blocking shape), the execution.py `blocking`-omission residual, the check_project_locked rename, and the new unguarded-startup-sweep P3."
  ],
  "checkpoint_reached": "CP4 (code); CP2 complete; CP3 code complete",
  "cp1_verified": "LIVE PASS (fbeb3ab) -- static_only superseded",
  "gates": {
    "verification": "CP1 live write check PASS (fbeb3ab); CP4 LCM premise verified live (evidence/live-cp4.md, re-run under flexicon 4.4.1 per db52c3a)",
    "qc": "Cycle 7 CLOSED by lex-lead ruling. Both cycle-6 P1s CLOSED; both cycle-7 P2s CLOSED (one fully, one accepted-with-residual); P3 rename DEFERRED with the contract documented in place. No P0, no open P1.",
    "pattern_audit_gate": "PASS (cycle 7). The mechanical artifact exists and satisfies every condition of the cycle-6 ruling: one row per lock-reading site (38), verbatim grep commands with raw hit counts that reproduce the 26/21/152 baseline exactly, a per-row blocking/non-blocking justification, and a re-verification protocol. It independently rediscovered both known defects and found two more. THE BARE-LOCK BUG CLASS IS CLOSED for src/ as enumerated, with two NAMED, documented residuals (execution.py's bare `locked` semantics; check_project_locked's misleading name) -- residuals, not unknowns. reviews/lock-site-inventory.md is a LIVING artifact: re-run its greps and diff the table before any future lock-related sweep claims completeness. Prose completeness claims remain inadmissible.",
    "p1_weakened_test": "RESOLVED",
    "domain": "PASS (cycle 2)",
    "scope_creep": "PASS",
    "live_write_check": "PASS (fbeb3ab) -- CP1 merge blocker cleared",
    "cp4_live_open_shared": "NOT RUN -- needs FieldWorks running and holding a sharing-enabled project; the 'visibly appears in the FLEx UI' half is a human observation (evidence/live-cp4.md section 6). This is the only red gate.",
    "cp3_live_sharing_off": "NOT RUN -- read-only run against a sharing-off, FLEx-open project must return the enable-sharing recipe (SPEC Verification step 5, final bullet). Human/live gate. Blocks CP3 SIGN-OFF only, not CP5/CP6 code work.",
    "spec_step6_regression": "PASS @ 7a4fa5c -- 1148 passed / 4 skipped (baseline 1144/4; delta is exactly the 4 new TestValidateOnlyProjectLockEnrichment cases, no regressions). validate_integrity.py all exit 0. lex-lead independently re-ran the 5 lock-related test files: 83 passed, 2 subtests passed.",
    "cp3_code": "ACCEPTED (CODE), UNCONDITIONAL -- the P1-1 atomicity condition from cycle 5 has landed (520dba4) and is QC-confirmed. SIGN-OFF still blocked on cp3_live_sharing_off (human/live only). The two new cycle-6 P1s do NOT reopen CP3: neither is in T3.1-T3.5's code path. Cycle 7 adds nothing to reopen it.",
    "cycle7_close": "ACCEPTED. No further verification pass over 7a4fa5c is required or wanted -- a fourth judgement review of a bug class that now has a mechanical artifact is precisely the waste the gate was created to end."
  },
  "commits": {
    "8a4fae5": "fix: writes silently fail under undoable=True; remove dead undo machinery (closes #92) -- d17105f amended to carry the pattern audit",
    "af79d81": "docs: record the CP1 review trail and deferred follow-ups (#93)",
    "fbeb3ab": "test: CP1 live write check for #92 PASSES -- binding merge blocker cleared",
    "39d1caf": "CP2 -- access probe (project_access.py) + 30 tests",
    "6dc459a": "CP4 -- shared-mode write gate; re-confirmed db52c3a under flexicon 4.4.1",
    "250ca52": "nested-UoW pre-flight gate (#92 follow-up)",
    "ae73eef": "Merge PR #114 -- shared-mode access + swahili-audit Bug 1",
    "6677fd8": "CP3 T3.1-T3.4 -- wire probe_project_access into project-locked diagnosis (local, unpushed)",
    "520dba4": "fix: CP3 diagnosis hardening (#93) -- local, unpushed",
    "ad1d50c": "fix: sweep the check_project_locked sibling for bare-lock false positives (#93) -- local, unpushed",
    "d0b360f": "docs: amend SPEC Section 3 + CP5 tasks ahead of implementation (#93) -- local, unpushed",
    "d278cc6": "docs: strike the resolved cast_to_concrete phantom-remedy blocker -- local, unpushed",
    "7a4fa5c": "fix: close cycle-7 P1s in shared-mode lock diagnosis (#93) -- local, unpushed",
    "HEAD (cycle-7 close)": "docs: close #93 cycle 7 -- both P1s fixed, pattern audit gate PASSES -- local, unpushed, docs-only"
  },
  "issues": {
    "cp1_bug": "#92 -- CLOSED (live write check passed; fix merged in PR #114)",
    "cp2_cp6_enhancement": "#93 -- OPEN (correctly: CP3 partial, CP5 unstarted, CP6 partial)"
  },
  "blocker": {
    "what": "The live FieldWorks session (specs/shared-mode-access/evidence/live-session-checklist.md, session 1). It is the only gate on CP3 and CP4 sign-off and the crew is barred from live-FLEx work.",
    "binding_item": "Checklist item 3 leg 3 -- a write with FLEx actually open appearing in the FLEx UI without a restart. Never verified. evidence/live-cp4.md proved only the LCM premise (a second process CAN write to a projectSharing=true project), not the open_shared code path under a real FLEx master.",
    "also_needs_the_user": "A push/PR authorization (see push_pr_ruling) and authorization to file the two deferred issues. Neither blocks CP5 code work.",
    "does_not_block": "CP5 implementation, CP6 docs, or the two P3 cleanups. A crew spurt can proceed on all of those with FieldWorks closed."
  },
  "merge_gate": "CLEARED -- PR #114 merged to main (ae73eef); git log origin/feat/shared-mode-access ^main is empty.",
  "deferred_no_issue_filed": [
    "[MED] execution.py:326 -- the liblcm-mode OpenProject stub ignores writeEnabled and has no undoable param. Dead today (_get_api_mode_imports has zero callers) but the same bug shape dormant; if rewired, CP1's hardcoded undoable=False call site would TypeError against it. Out of CP1 scope.",
    "[LOW] execution.py:3345 -- FlexToolsModule.Run modifyAllowed=False default; sole call site always passes WRITE_ENABLED explicitly. Safe-direction, unreachable.",
    "[LOW] index/common_patterns_flexicon-v4.3.0.json:1271 -- flexicon's own CreateField docstring documents an identical landmine. Not our code; FYI.",
    "[P2, pre-existing] SPEC.md Section 8 -- docs/TOOL-CONTRACT.md:13-26 claims all success responses carry _contract/status/op_id, but run_module's raw success dict never does. Predates CP1. NEEDS THE USER'S AUTHORIZATION TO FILE.",
    "[P1-class, pre-existing] The FAIL-OPEN PROBE -- probe_project_access verdict='free' on an unresolvable projects directory. Interim patch LANDED (probed flag); the full fix is the verdict='unknown' enum widening. NEEDS THE USER'S AUTHORIZATION TO FILE -- see fail_open_ruling.recommended_shape_when_filed for the exact three-part shape.",
    "[P3, cycle 7] check_project_locked -> find_lock_file rename. No issue wanted; do it in the CP6 cleanup pass. See p1_closed_cycle7.P3_check_project_locked_rename.",
    "[P3, cycle 7, NEW] sweep_stale_locks() runs at server startup (server.py:1048) with no try/except around its per-lock loop and none at the call site -- an unexpected exception there fails server startup. Pre-existing shape; cycle 7 added two internally-safe calls inside the loop. One-line fix in the CP6 cleanup pass."
  ],
  "checkpoints_remaining": {
    "CP2": "DONE -- 39d1caf",
    "CP3": "CODE ACCEPTED (unconditional) and cycle-7 hardened [6677fd8, 520dba4, ad1d50c, 7a4fa5c]. SIGN-OFF blocked ONLY on live-session-checklist item 4.",
    "CP4": "CODE DONE. SIGN-OFF blocked ONLY on live-session-checklist items 1-3, of which item 3 leg 3 (visible in the FLEx UI without a restart) is the binding one.",
    "CP5": "SCOPED (cycle5-cp5-scope.md) + SPEC AMENDED [d0b360f]. NOT IMPLEMENTED -- this is the next CREW work, and it is NOT blocked by the live session: implement a CUSTOM-FIELDS-ONLY core (T5.2-T5.6) against the one certain Class B row. Writing systems / possibility lists / reversal indexes are ROWS added later without touching the gate; live items 5-7 only classify those rows. Rulings in cp5_rulings stand.",
    "CP6": "PARTIAL -- T6.5 (CHANGELOG) done; docs/SHARED-MODE.md does not exist; T6.1-T6.4, T6.6, T6.7 outstanding"
  },
  "next_checkpoint": "CP5 -- exclusive-access gate (custom-fields-only core), T5.2-T5.6",
  "next_entry": "Cycle 7 is CLOSED GREEN and the spurt ends on a HUMAN GATE. Three asks are queued for the user, in priority order: (1) run specs/shared-mode-access/evidence/live-session-checklist.md session 1 -- one FieldWorks sitting, 8 items, backup first; (2) authorize the push + PR per push_pr_ruling (six green commits are unpushed on main); (3) authorize filing the two issues in deferred_no_issue_filed marked 'NEEDS THE USER'S AUTHORIZATION' (the fail-open probe, and the TOOL-CONTRACT run_module envelope gap). The next CREW spurt -- which needs NONE of the above -- implements CP5 T5.2-T5.6 (custom-fields-only core) against the amended SPEC, then CP6 docs plus the two P3 cleanups (check_project_locked rename, guard the startup sweep loop).",
  "updated": "2026-09-07",
  "spec_kit_note": "This file is AUTHORITATIVE for the feature. specs/shared-mode-access/.spec-context.json and the spec-kit companion resolver are unreliable here -- ignore them.",
  "cp5_rulings": {
    "justification": "UPHELD, and sharper than SPEC states. Class B (LCM permits the write and silently swallows it -- custom fields) is the class that needs a gate; Class A (LCM refuses outright -- migration, rename) is safe by construction. Precedent: FieldWorks gates this identically in its own UI (XWorksViewBase.cs:715, AreMultipleApplicationsConnected).",
    "needs_lock_seam": "RULED: detect FIRST, probe SECOND. If detect_exclusive_only_operations() matches, the CP5 gate must force the access probe regardless of needs_lock -- a script that certifies read-only while calling CreateField is mis-certified, and the gate must not be bypassable through that hole.",
    "precedence": "RULED: CP3's enable-sharing remedy WINS over CP5 for an exclusively-held project. The CP5 check keys on verdict == 'open_shared' / _live_fw_peer (execution.py:4144), never on '_access is not None'.",
    "insertion_point": "execution.py:4262 -- after the CP4 project_locked refusal (:4227-4261), before the open_shared advisory (:4263-4299), before backup (:4301).",
    "new_task_T5_6": "Align the generic post-hoc fallback at execution.py:1207-1213 ('Close FieldWorks and retry') so it does not collide with CP5's message on an open_shared project. build_lock_diagnosis() returns None for open_shared."
  },
  "spec_drift_to_fix_before_T5_1": [
    "T5.5 says bump the '16 codes' wording -- it is 18, in three lockstep places: docs/TOOL-CONTRACT.md:69, tests/test_response_contract.py:9 and :234 (+ ALL_ERROR_CODES at :200-231). CP5 makes 19. NOTE: response_models.py:361 was cited by research but grep shows the count wording lives in the two files above -- programmer must re-grep before editing.",
    "T5.1 names CustomFieldOperations.UpdateField, which does not exist in flexicon 4.5.2. SetFieldName is the schema-mutating analogue.",
    "T5.1's raw-LCM names (AddCustomField, UpdateCustomField, RenameDatabase, FieldDescription) have zero occurrences in src/ -- reachable only via user-submitted code, so T5.2 must detect them by AST, not from any existing table.",
    "Section 3 rows that are UNREACHABLE from this MCP (mark as such, do not gate): data migration (flexlibs sets DisableDataMigration=True), Send/Receive, project backup/restore/delete.",
    "Add failure_class: refused | silently_lost as a column, and add possibility lists + reversal indexes as explicit 'unclassified -- pending live test' rows. Silence reads as 'vetted' to a future maintainer.",
    "Stale pointer: project_access.py:283 cites the write gate as execution.py:4180; it is :4203-4299."
  ],
  "live_session_checklist": {
    "AUTHORITATIVE_FILE": "specs/shared-mode-access/evidence/live-session-checklist.md -- the single ordered, executable checklist for ONE FieldWorks sitting, with exact commands and per-item pass criteria. Use that file, not this summary.",
    "session_1_covers": [
      "0. Pre-flight: pick the sharing-ON project (Target or Claude-Swahili), TAKE A RESTORABLE BACKUP FIRST (items 5-7 are schema-level), pick/prepare a sharing-OFF project, restart the MCP server so it is running the tree through 7a4fa5c.",
      "1. CP4-a: flextools_health(verbose=True) -> verdict open_shared, sharing_enabled true, holder_pid == the REAL FieldWorks PID (cross-check tasklist).",
      "2. CP4-b: read-only run_module listing 5 entries succeeds with no 'Close FieldWorks' text.",
      "3. CP4-c: write run_module setting a gloss -> not refused, report shows the change, AND the new gloss is visible in the FLEx UI after F5 WITHOUT restarting FLEx. THE SINGLE MOST LOAD-BEARING OBSERVATION IN THE FEATURE. If legs 1-2 pass but leg 3 fails, STOP before item 5 and return to the crew -- that is the #96 commit-log staleness shape and CP4's 'writes are expected to succeed' wording must be re-ruled before CP5 is built on it.",
      "4. CP3: on the sharing-OFF project with FLEx open, a read-only run must return the ENABLE-SHARING RECIPE, not the generic lock hint; then enable sharing, reopen FLEx, re-run -> proceeds with no prompting.",
      "5-7. OPEN Q1 writing systems / Q2 possibility-list items / Q3 reversal indexes: for each, write with FLEx open, check the UI, THEN close and reopen FLEx and check again. Before-vs-after-restart is the whole finding: visible-then-gone == silently_lost == the class CP5 must gate.",
      "8. Wrap-up: append verbatim output to evidence/live-cp4.md, restore the backup if items 5-7 damaged anything, hand back to the crew."
    ],
    "session_2_later": "CP5-a only, ~10 minutes, ONLY after the CP5 gate code lands: CustomFieldOperations.CreateField refused with requires_exclusive_access -> close FLEx -> identical call succeeds -> reopen FLEx and confirm the field exists."
  },
  "dispatch_infrastructure_bug": "subagent_type 'lex-qc' DOES NOT RESOLVE (confirmed AGAIN in cycles 5 and 6 -- the main session had to substitute both times). The Agent registry rejects it even though ~/.claude/agents/lex-qc.md exists with valid frontmatter; probable cause is the same-named skill and ~/.claude/commands/lex-qc.md shadowing the agent registration. STANDING RULE: lex-lead MUST NOT emit subagent_type 'lex-qc'. Dispatch subagent_type 'general-purpose' instructed to read ~/.claude/agents/lex-qc.md and adopt that rubric verbatim. The same substitution applies to any other lex-* type that fails to resolve.",
  "fail_open_ruling": {
    "finding": "probe_project_access:351-359 -- get_projects_directory() returning None yields verdict='free', sharing_enabled=None, so a host without FieldWorks/registry/env reports blocking:false WITHOUT EVER PROBING (cycle6-qc.md P2-3).",
    "scope": "NOT CP3, NOT CP4, NOT a regression. NEW ISSUE (needs the user's authorization to file).",
    "rationale": "(1) Pre-existing since issue #33: check_project_locked (project_discovery.py:262-274) has returned None -> 'not locked' -> proceed on dir_result None since long before the CP series; ad1d50c did not introduce it. (2) NO data-loss path: if the projects directory cannot be resolved, the subprocess cannot resolve the project either, so the write fails at LCM open time and lands in CP3's post-hoc _diagnose_project_open_error. LCM remains the backstop; the gate is vacuous, not bypassed. (3) The real fix -- a 6th verdict 'unknown' / blocking:null -- widens an enum consumed by build_access_remedy, build_lock_diagnosis, the CP4 gate, diagnostic_health, ProjectLockedDetail and 30+ tests. Folding an enum widening into the tail of a hardening cycle is precisely the unbounded drift that has already cost two cycles.",
    "recommended_shape_when_filed": "STILL NEEDS THE USER'S AUTHORIZATION TO FILE. Three parts: (1) keep the landed additive `probed: bool = True` on ProjectAccess as the seam; (2) add a real verdict='unknown' with blocking:null ONLY if a consumer actually needs to branch on it -- it widens an enum consumed by build_access_remedy, build_lock_diagnosis, the CP4 gate, diagnostic_health, ProjectLockedDetail and 30+ tests, which is why it was never folded into a hardening cycle; (3) make every reporting site emit `blocking` UNCONDITIONALLY alongside `locked`, plus a `note` stating that `locked` is bare lock-file existence (this absorbs the cycle-7 P2 residual). Recorded so it is not relitigated.",
    "interim_patch_ruled_IN_for_cycle_7": "ad1d50c NEWLY PUBLISHES `blocking` in the validate_only payload. We own not shipping a brand-new field that asserts false confidently. Cycle 7 must emit blocking:null (and omit the verdict) when the probe did not actually probe. Scope is the reporting sites + the ProjectAccess flag ONLY -- the CP4 gate's dispatch is NOT to be touched.",
    "interim_patch_status": "LANDED [7a4fa5c]. ProjectAccess.probed: bool = True exists, False only in the dir-unresolvable branch; the validate_only site emits blocking=None and omits verdict when probed is False. Verdict enum and CP4 gate dispatch untouched, as ruled."
  },
  "p1_closed_cycle7": {
    "P1-A_enrichment_untested": "CLOSED [7a4fa5c] -- probe_project_access stubbed in _stub_validate_only_env + _stub_agreement_env; TestValidateOnlyProjectLockEnrichment covers free/open_shared/open_exclusive/unprobed. The seam is no longer host-dependent.",
    "P1-B_missed_sibling_project_discovery": "CLOSED [7a4fa5c] -- unknown-holder branch now mirrors build_access_remedy's unknown-holder text verbatim ('could not be identified... Treating it as exclusively held'), drops 'Stale' and the FieldWorks attribution; the test that pinned the old wording was renamed and now asserts the corrected text plus a negative assertion on 'Stale lock detected'.",
    "P2_project_discovery_344_354": "CLOSED [7a4fa5c] -- live-holder branch branches on _is_fieldworks_process_name and is_project_sharing_enabled; held_by_other wording delegated to build_access_remedy so it cannot drift from CP3/CP4.",
    "P2_execution_2035_2047": "CLOSED WITH RESIDUAL [7a4fa5c] -- `blocking` is now published in the probed path and null when unprobed. `locked` remains bare existence, and `blocking` is still omitted when project_name is falsy or the probe raises (safe-direction: a consumer over-blocks). The inventory's verdict=\"unknown\" + `note` half was correctly NOT done -- it is the deferred enum widening. Folded into the fail-open issue, item (3).",
    "P3_check_project_locked_rename": "DEFERRED, NO ISSUE FILED, and that is sufficient -- the memory is durable three ways: a row in reviews/lock-site-inventory.md, the corrected contract in the function's own docstring, and SPEC.md Section 8. A micro-issue for a zero-behaviour-change rename inside an already-open #93 would be noise. Schedule it as a standalone mechanical commit in the CP6 cleanup pass (~9 call sites across src/ and tests/)."
  },
  "push_pr_ruling": {
    "ruling": "PUSH AND OPEN A PR NOW -- do NOT wait on the live gate. NOT YET AUTHORIZED: the user has not authorized a push or a PR in this session, so the main session must ASK before doing any of it.",
    "why_not_wait": "None of the six commits' correctness depends on the live observation. The live gate blocks CP3/CP4 SIGN-OFF, which is issue-tracked prose, not code state. Holding seven green commits as unpushed local work on `main` for days is the largest loss-of-work risk in the current tree, and it is a strictly worse risk than an open PR that says 'live sign-off pending'.",
    "recommended_mechanics": "The commits sit on `main`, which bypasses the repo's PR-per-change convention (PRs #114/#116/#117). Recommended: (1) branch `fix/shared-mode-lock-diagnosis-93` at the current tip; (2) reset local `main` back to origin/main (433f3a0); (3) push the branch and open a PR referencing #93 but NOT closing it (CP5 and CP6 remain). The PR body should state plainly that CP3/CP4 code is green and unit-proven while their live sign-off is pending the session-1 checklist.",
    "do_not": "Do not force-push, do not push directly to main, and do not let any loop do this unattended -- it needs the user's explicit yes."
  },
  "environment_note_cycle7": "The installed `flexicon` resolves to the SIBLING SOURCE REPO at D:\\Github\\_Projects\\_LEX\\flexicon\\flexicon (editable/source install, branch main at 7e0fdf2, PAST the 4.5.2 release), NOT a site-packages wheel. CLAUDE.md states flexicon is PyPI-only and 'no longer a cloned sibling repo' -- that is NOT what this machine runs. Consequence: any claim of the form 'verified against flexicon 4.5.2' on this machine is really 'verified against main@7e0fdf2'. Re-check before trusting the index migration (accd5a1) or any live evidence file's version line, and state the resolved path explicitly in future live evidence."
}
